import { CentimetersToMetersPipe } from './centimeters-to-meters.pipe';

describe('CentimetersToMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new CentimetersToMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
